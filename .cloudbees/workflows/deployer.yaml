apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:
jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: upload-Bin
          artifact-id: 769b38b6-9d62-4bd4-8b12-151c973546fc
          target-environment: Pre-Prod
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: ecr
          artifact-id: ed16110b-c681-4b30-939b-724c0c3f85ca
          target-environment: Pre-Prod